name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node-version: [20, 22]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - run: npm test
      # Keep installs green: block CI on high+ advisories, let moderates through.
      - run: npm audit --audit-level=high

  # Stable aggregation gate: branch protection requires only this one check, so
  # editing the test matrix (OS/node) never orphans a required check name.
  ci-passed:
    name: ci-passed
    if: always()
    needs: [test]
    runs-on: ubuntu-latest
    steps:
      - name: Fail if any test leg did not succeed
        if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') || contains(needs.*.result, 'skipped')
        run: |
          echo "one or more test jobs did not succeed: ${{ join(needs.*.result, ', ') }}"
          exit 1
